% LM-KBC 2026 shared-task system paper.
% Build: cd paper && tectonic main.tex   (acl.sty + acl_natbib.bst vendored here)
% NB: this shows the author (mode = final). For the DOUBLE-BLIND OpenReview
% submission, change [final] to [review] to re-anonymize the author block.
% NB: do NOT add \bibliographystyle{acl_natbib} -- acl.sty sets it (duplicate => bibtex error).
\documentclass[11pt]{article}
\usepackage[final]{acl}
\usepackage{times}
\usepackage{latexsym}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{enumitem}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,backgrounds,calc}
\definecolor{cIn}{HTML}{E8EEF7}\definecolor{cPrompt}{HTML}{DCEAF7}
\definecolor{cDecode}{HTML}{EFEFEF}\definecolor{cAgg}{HTML}{E2F0E2}
\definecolor{cOut}{HTML}{FBE9D6}\definecolor{cBadge}{HTML}{FFF6D5}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{algorithm}
\usepackage{algpseudocode}
\emergencystretch=3em  % absorb long \textsc relation names without margin overflow

\title{Closed-Book Knowledge Base Construction with Calibrated Abstention
and Numeric Self-Consistency}
\author{Tanvir Ahmed \\
  North South University, Dhaka, Bangladesh \\
  \texttt{tanvir.ahmed32@northsouth.edu}}

\begin{document}
\maketitle

\begin{abstract}
Large language models (LMs) store a remarkable amount of factual knowledge in
their parameters, yet turning that knowledge into a reliable, \emph{set-valued}
knowledge base---where a subject may have zero, one, or many objects---remains
difficult. The LM-KBC~2026 shared task (5th edition, at the AKBC workshop,
EMNLP~2026) poses exactly this in a strict \emph{closed-book} setting: an
open-weight model of at most 32B parameters, with no retrieval, no external
knowledge, and no fine-tuning. We start from the evaluation metric and observe
that it rewards \emph{abstention}: predicting the empty set for every instance
already scores $0.175$ relation-averaged macro-F\textsubscript{1}, close to the
official baseline ($0.273$). We therefore treat \emph{when to answer} as a
first-class decision and build a per-relation pipeline that (i)~grounds each
prompt in the relation's exact scope, (ii)~induces calibrated abstention by
\emph{self-consistency voting}---abstaining when repeated samples
disagree---rather than by instruction, and (iii)~aggregates numeric relations by
median. With \textbf{Gemma-4-31B} (a 30.7B open-weight model, the strongest in
the budget) the system reaches $0.459$ relation-averaged macro-F\textsubscript{1}
($0.490$ micro), beating the official baseline ($0.273$/$0.284$) on all six
relations; a smaller Qwen2.5-14B run locally in 4-bit reaches $0.416$. All
generation is cached, so the entire decode stage is reproducible without a GPU.
We release our
code.\footnote{\url{https://github.com/ANON/lm-kbc-2026}}
\end{abstract}

\section{Introduction}
Pre-trained language models (LMs) store a remarkable amount of factual knowledge
in their parameters, acquired during pre-training on large text corpora
\citep{petroni2019lama,roberts2020closedbook}, and a body of work probes and
extracts it \citep{jiang2020lpaqa,zhong2021optiprompt,alkhamissi2022review}.
This raises the prospect of building a structured knowledge base (KB) directly
from an LM rather than from curated sources \citep{dong2014knowledgevault,vrandecic2014wikidata}.
Doing so reliably is harder than answering a single factual question: a KB entry
is a \emph{complete set} of facts, so the model must decide not only what is true
but how many objects---possibly none---a subject and relation admit.

\paragraph{The LM-KBC 2026 task.} The shared task
\citep{alivanistos2022prompting,kalo2023lmkbc,kalo2025lmkbc} formalises this as:
given a subject $s$ and relation $r$, predict the complete object set
$\{o_1,\dots,o_k\}$, where $k$ may be zero, one, or many. Predictions are scored
by macro precision, recall, and F\textsubscript{1}, with string normalisation
and alias matching for entities and a 5\% tolerance for numeric relations. The
2026 edition is strictly \emph{closed-book}: a system may use any open-weight
model up to 32B parameters, but no web search, no retrieval, no external corpus,
and no fine-tuning. This rules out the retrieval and tuning that strong entries
in earlier editions relied on \citep{zhang2023llmke,asai2024selfrag}, leaving
parametric recall and \emph{prompt design} as the only levers.

\paragraph{Why this is hard.} Unlike single-answer probing benchmarks such as
LAMA \citep{petroni2019lama}, which assume exactly one gold object and reward
ranking it highly, this setting demands the \emph{entire} set---and the empty
set is a legitimate, frequent answer (a living person has no recorded city of
death; an island country has no land border). Three difficulties follow. First,
LM outputs are stochastic and prompt-sensitive, so a single query yields
unstable sets. Second, relations span very different cardinalities, from a single
number to hundreds of award recipients, complicating aggregation. Third, the
model must recognise when it does not know an answer and \emph{abstain} rather
than fabricate one.

\paragraph{Our approach.} We start from the evaluation metric itself. Because
macro-F\textsubscript{1} scores an empty prediction against an empty gold set as
perfect, simply predicting nothing everywhere already attains $0.175$
relation-averaged macro-F\textsubscript{1} on validation---competitive with the
official baseline ($0.273$) and better on the most null-heavy relations
(Section~\ref{sec:task}). The lesson is that points come not from answering
everything, but from recalling the few numeric values and, above all, knowing
when \emph{not} to answer. We therefore make abstention a first-class decision:
we sample each null-heavy relation several times and keep an object only when the
samples agree, collapsing to the empty set when the model is internally
inconsistent. Scope-grounded prompts and median aggregation for numeric relations
complete the pipeline (Figure~\ref{fig:pipeline}).

\medskip\noindent Our contributions are:
\begin{enumerate}[noitemsep,topsep=2pt,leftmargin=*]
  \item A metric analysis (Section~\ref{sec:task}) showing that, under the macro
  metric, abstention is a high-value action: the predict-nothing baseline already
  rivals the official baseline, which reframes the task around \emph{when to
  answer}.
  \item A per-relation pipeline (Figure~\ref{fig:pipeline}, Section~\ref{sec:method})
  that grounds prompts in exact scopes and induces \emph{calibrated abstention}
  via self-consistency voting, with no trained confidence model: it lifts a weak
  0.5B model from $0.173$ to $0.372$ on the null-heavy relations and still helps
  a strong 14B.
  \item A reproducible open-weight system reaching $0.459$ relation-averaged
  macro-F\textsubscript{1} with Gemma-4-31B (0.416 with a local 4-bit
  Qwen2.5-14B), beating the official baseline on all six relations, with cached
  generation enabling GPU-free decode-stage tuning
  (Sections~\ref{sec:setup}--\ref{sec:analysis}).
\end{enumerate}

\section{Background and Related Work}
\paragraph{LMs as knowledge bases.} \citet{petroni2019lama} cast factual recall
as cloze probing; follow-ups sharpened the prompts \citep{jiang2020lpaqa,
shin2020autoprompt,zhong2021optiprompt}, quantified how much knowledge a model's
parameters hold \citep{roberts2020closedbook}, and questioned how reliably it can
be read out \citep{cao2021knowledgeable,alkhamissi2022review}. These probes
assume a single gold object and reward ranking it highly. We instead target the
set-valued, alias-graded variant in which the empty set is a valid answer, which
is closer to materialising an actual KB \citep{pan2023llmkg}.

\paragraph{The LM-KBC shared task.} Across its editions
\citep{alivanistos2022prompting,kalo2023lmkbc,kalo2024lmkbc,kalo2025lmkbc} the
task has moved from masked-LM probing and prompt ensembling toward
instruction-tuned generative systems, with some earlier tracks permitting
retrieval or fine-tuning \citep{zhang2023llmke}. The 2026 edition's closed-book,
no-tuning, $\le$32B rules remove those options---which is precisely what makes a
parametric-recall-plus-abstention design necessary.

\paragraph{Retrieval vs.\ closed book.} The usual route to knowledge-intensive
accuracy is retrieval augmentation \citep{lewis2020rag,guu2020realm}, which helps
most exactly when parametric memory is weak \citep{mallen2023whennot}. Self-RAG
\citep{asai2024selfrag} even lets the model decide \emph{when} to retrieve; we
borrow the spirit of a model-driven, adaptive decision but, with retrieval
forbidden, apply it to a different one---\emph{when to answer at all}, not what to
fetch.

\paragraph{Self-consistency, calibration, and abstention.} These threads converge
in our method. Self-consistency \citep{wang2022selfconsistency,chen2023universalsc}
samples a model repeatedly and aggregates the generations to pick a single
answer. Abstaining when uncertain is the classic reject option
\citep{geifman2017selective}, studied in NLP as calibration
\citep{guo2017calibration}, unanswerable QA \citep{rajpurkar2018squad2}, and
selective QA \citep{cole2023selective}, and LMs retain some awareness of what
they know \citep{kadavath2022know}. We combine the two: cross-sample
\emph{disagreement} \citep{kuhn2023semantic,manakul2023selfcheckgpt} is our
zero-parameter confidence signal, and---unlike majority-vote self-consistency,
which always emits an answer---we let it trigger \emph{abstention}, because here
the empty set is itself a high-value prediction.

\section{Task and Metric Analysis}
\label{sec:task}
\paragraph{Relations.} The dataset comprises six Wikidata relations
(Table~\ref{tab:data}) that fall into three groups. \emph{Numeric} relations
(\textsc{hasCapacity}, \textsc{hasArea}) have a single scalar object;
\emph{large-set} \textsc{awardWonBy} has up to hundreds of recipients per
subject; and three \emph{null-heavy} string relations
(\textsc{countryLandBordersCountry}, \textsc{personHasCityOfDeath},
\textsc{companyTradesAtStockExchange}) leave a large fraction of subjects with no
valid object at all. Each relation carries a precise scope---land-only borders,
city-granular death location, the highest published capacity---which we copy
verbatim into the prompt so the model targets the same notion the gold set was
built against.

\begin{table}[t]
\centering\small
\begin{tabular}{lrr}
\toprule
Relation & \#val & empty\% \\
\midrule
\textsc{countryLandBordersCountry} & 68 & 25 \\
\textsc{personHasCityOfDeath} & 100 & 45 \\
\textsc{companyTradesAtStockExchange} & 100 & 35 \\
\textsc{awardWonBy} & 10 & 0 \\
\textsc{hasCapacity} & 100 & 0 \\
\textsc{hasArea} & 100 & 0 \\
\bottomrule
\end{tabular}
\caption{Validation statistics. \textsc{awardWonBy} averages ${\sim}148$ objects
per subject (max 641); three string relations are often legitimately empty.}
\label{tab:data}
\end{table}

\paragraph{Evaluation.} Predictions are scored against the gold object set by
macro precision and recall. A predicted string is a true positive if its
normalised form (lower-cased, diacritics and punctuation removed) matches any
alias of a gold object, and a numeric prediction if it lies within 5\% of the
gold value. Two boundary conventions drive everything that follows: precision is
defined as $1$ when the prediction is empty, and recall as $1$ when the gold set
is empty. We report \emph{relation-averaged} macro-F\textsubscript{1} (the mean
of the six per-relation scores) and \emph{micro-averaged} F\textsubscript{1}
(over all instances).

\paragraph{The metric makes abstention valuable.} Those two conventions make an
empty prediction against an empty gold set a perfect F\textsubscript{1}, with a
large consequence: predicting the empty set for \emph{every} validation instance
already scores the ``Empty'' bars in Figure~\ref{fig:bars}, averaging $0.175$
relation-averaged macro-F\textsubscript{1} ($0.203$ micro). This is on par with
the official baseline's $0.273$ and \emph{above} it on the two most null-heavy
relations (\textsc{personHasCityOfDeath} $0.45$ vs.\ $0.16$;
\textsc{companyTradesAtStockExchange} $0.35$ vs.\ $0.32$). The task therefore
rewards not answering everything, but two narrower abilities: recalling the few
numeric values, and abstaining precisely when no object exists. These are the
levers our pipeline pulls.

\begin{figure*}[t]
\centering\footnotesize
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
 >={Stealth[length=2.6mm]}, font=\footnotesize,
 stg/.style={draw,rounded corners,align=center,inner sep=4pt,minimum height=13mm,text width=20mm},
 agg/.style={draw,rounded corners,align=center,inner sep=3pt,minimum height=9mm,text width=27mm,fill=cAgg,font=\scriptsize},
 dia/.style={draw,diamond,aspect=1.6,align=center,inner sep=0pt,font=\scriptsize,fill=cDecode,text width=11mm},
 num/.style={circle,draw,fill=black!80,text=white,font=\bfseries\tiny,inner sep=1pt,minimum size=4mm},
 call/.style={draw,dashed,rounded corners,fill=cBadge,inner sep=2.5pt,font=\scriptsize\itshape,text=black!70}]
\node[stg,fill=cIn] (in){$(s,r)$};
\node[stg,fill=cPrompt,right=9mm of in] (pr){scope-grounded\\prompt};
\node[stg,fill=cDecode,right=9mm of pr] (dec){LLM $\times N$\\\textbf{Gemma-4-31B}};
\node[dia,right=8mm of dec] (d){relation\\kind?};
\node[agg,above right=9mm and 9mm of d] (awd){\textsc{awardWonBy}: enumerate};
\node[agg,right=9mm of d] (num){numeric: median of $n$};
\node[agg,below right=9mm and 9mm of d] (nul){null-heavy: vote $\ge\theta$, else $\emptyset$};
\node[stg,fill=cOut,right=9mm of num,text width=16mm] (norm){normalise};
\node[stg,fill=cOut,right=7mm of norm,text width=13mm] (out){object set\\\texttt{\{\dots\}}};
\draw[->](in)--(pr);\draw[->](pr)--(dec);\draw[->](dec)--(d);
\draw[->](d)|-(awd);\draw[->](d)--(num);\draw[->](d)|-(nul);
\draw[->](awd)-|(norm);\draw[->](num)--(norm);\draw[->](nul)-|(norm);
\draw[->](norm)--(out);
\foreach \n/\nd in {1/in,2/pr,3/dec,4/num,5/norm,6/out}{\node[num] at (\nd.north west){\n};}
\node[call,above=5mm of awd]{Nobel Prize in Physics $\to$ \{Einstein, Bohr, \dots\}};
\node[call,below=5mm of nul]{Mauritius $\to \emptyset$ (island: no land border)};
\node[call,below=13mm of dec,text width=42mm]{empty pred vs.\ empty gold $=$ perfect F$_1$ $\Rightarrow$ \textbf{abstain when unsure}};
\node[font=\scriptsize,below=15mm of in,text width=46mm,align=left] (leg)
 {\textbf{stages:}\ \tikz\fill[cPrompt]circle(1.1mm); prompt\ \ \tikz\fill[cDecode]circle(1.1mm); decode\ \ \tikz\fill[cAgg]circle(1.1mm); aggregate\ \ \tikz\fill[cOut]circle(1.1mm); output};
\node[draw,rounded corners,fill=cAgg,inner sep=3pt,font=\scriptsize\bfseries,below right=13mm and 0mm of norm]
 {val: 0.459 avg-F$_1$ / 0.490 micro};
\end{tikzpicture}}
\caption{System pipeline (no trainable parameters). Each $(s,r)$ is rendered into
a scope-grounded chat prompt (stage~2), the model is sampled $N$ times
(stage~3, cached), and the samples are aggregated by relation kind (stage~4):
\emph{median} for numeric relations, consistency \emph{voting with abstention}
(keep an object only if it appears in $\ge\theta$ of samples, else $\emptyset$)
for the null-heavy string relations, and \emph{enumeration} for
\textsc{awardWonBy}; a final normalisation matches the evaluator exactly. Dashed
boxes are worked examples---the island \textsc{Mauritius} correctly collapses to
$\emptyset$. The design follows directly from the metric insight (lower-left
badge). Reported score is Gemma-4-31B on validation.}
\label{fig:pipeline}
\end{figure*}

\section{System}
\label{sec:method}
\paragraph{Architecture overview.} The pipeline (Figure~\ref{fig:pipeline}) maps
each $(s,r)$ to one chat prompt, decodes $N$ or $n$ cached samples, and aggregates them
per relation kind into an object set. It has no trainable parameters and targets
the two analysis levers: abstention and numeric recall.

\paragraph{Scope-grounded prompts.} Each relation's system message states the
task, the \emph{exact} ground-truth scope (copied verbatim from the official
definitions), and the required answer form (Table~\ref{tab:prompts}); the model
emits one JSON object \texttt{\{"answers":[\dots]\}} (Figure~\ref{fig:prompt}),
which makes the empty case unambiguous. Few-shot exemplars come from the training
split; for null-heavy relations we interleave empty and non-empty exemplars so
abstention is demonstrated, not merely instructed. Even a 0.5B model emits valid
JSON on 100\% of instances, so format is never a bottleneck.

\begin{table*}[t]
\centering\footnotesize
\setlength{\tabcolsep}{5pt}
\begin{tabular}{@{}l p{7.7cm} l@{}}
\toprule
Relation & Scope stated in the prompt & Answer form \\
\midrule
\textsc{countryLandBordersCountry} & countries sharing a \emph{land} border; maritime borders excluded; an island has none & countries, or [] \\
\textsc{personHasCityOfDeath} & the \emph{city} where the subject died; empty if alive or the city is unknown & one city, or [] \\
\textsc{companyTradesAtStockExchange} & stock exchange(s) on which the company is publicly listed; empty if unlisted & exchanges, or [] \\
\textsc{awardWonBy} & every recipient of the award (often tens to hundreds) & names (long) \\
\textsc{hasCapacity} & maximum spectator capacity in people; highest published value & one integer \\
\textsc{hasArea} & total surface area in km$^2$ (land $+$ inland water) & one number \\
\bottomrule
\end{tabular}
\caption{Per-relation prompt specification. Each system message states the
relation's exact scope (copied from the official definitions) and the required
answer form; the model replies with a single JSON object
\texttt{\{"answers":[\dots]\}}.}
\label{tab:prompts}
\end{table*}

\begin{figure}[t]
\centering\scriptsize\ttfamily
\fbox{\parbox{0.86\columnwidth}{%
SYS: precise KB engine (closed book).\\
TASK: city where the subject died.\\
SCOPE: city granularity; [] if alive/unknown.\\
Reply only \{"answers":[...]\}.\\[2pt]
U: Egbert Mulder\\ \ \ A: \{"answers":["Groningen"]\}\\
U: Dave Keon\\ \ \ A: \{"answers":[]\}\\
U: \textit{<query>}\ \ A:
}}
\caption{Abbreviated prompt for a null-heavy relation. Mixed exemplars teach
abstention; output is forced JSON.}
\label{fig:prompt}
\end{figure}

\paragraph{Calibrated abstention via self-consistency.} Scope grounding tells the model \emph{what} counts as an answer, but not \emph{when} to stay silent. A bare instruction to
``answer empty when unsure'' is unreliable: an unconstrained small model names a
death-city for living people and invents stock exchanges, dropping precision
\emph{below} the empty baseline (Section~\ref{sec:analysis}). Instead, for the
three null-heavy relations we sample $N{=}3$ completions at temperature
$\tau{=}0.7$ and keep an object only if it appears in at least a fraction
$\theta$ of samples; single-object relations abstain entirely if even the top
answer is below $\theta$ (Algorithm~\ref{alg:abstain}). When the model guesses,
samples disagree and the prediction collapses to $\emptyset$---where the metric
rewards silence. This is disabled for \textsc{awardWonBy} (recall-bound) and the
numeric relations.

\begin{algorithm}[t]
\small
\caption{\textsc{CalibratedAbstention}}
\label{alg:abstain}
\begin{algorithmic}[1]
\Require samples $S{=}\{s_1,\dots,s_N\}$, threshold $\theta$, \textit{multi}
\State $c[o] \gets \#\{s_i : o \in \textsc{parse}(s_i)\}$ for each object $o$
\If{\textit{multi}} \Return $\{o : c[o]/N \ge \theta\}$
\Else
  \State $o^* \gets \arg\max_o c[o]$;\ \ \Return $c[o^*]/N \ge \theta \,?\, \{o^*\} : \emptyset$
\EndIf
\end{algorithmic}
\end{algorithm}

\paragraph{Numeric aggregation.} The numeric relations need a different aggregator. \textsc{hasCapacity} and \textsc{hasArea} take a
single number graded within 5\%. We sample $n{=}5$ completions and take the
\emph{median} of the parsed numbers \citep{wang2022selfconsistency} for
robustness.

\paragraph{Robust parsing.} Whatever the relation kind, every sample passes through a common final stage. A relation-aware parser recovers the answer list from
noisy output (code fences, prose), strips explicit empty markers, extracts one
number for numeric relations (handling commas, units, scale words), and
de-duplicates. Normalisation (lower-casing, diacritic/punctuation removal) is
byte-identical to the official evaluator; pushing gold answers back through the
parser scores $1.000$, confirming it is lossless.

\section{Experimental Setup}
\label{sec:setup}
\paragraph{Models.} Our primary system uses \textbf{Gemma-4-31B-it}
\citep{gemma2024}, a 30.7B-parameter open-weight model within the 32B cap.
Because it exceeds the memory of the single 16\,GB GPU available to us, we serve
it through a hosted OpenAI-compatible API; the weights are open and no
fine-tuning is applied, so the closed-book rules are respected (the API is used
purely as compute). For the self-consistency ablations
(Section~\ref{sec:analysis}) we additionally run \textbf{Qwen2.5-14B-Instruct}
\citep{qwen2024} (14.7B) locally in 4-bit. Several nominally ``32B'' models in
fact exceed the cap (Qwen2.5-32B at 32.5B, Qwen3-32B at 32.8B) and are ineligible.

\paragraph{Decoding.} The pipeline selects an aggregator per relation kind
(Section~\ref{sec:method}): median over $n{=}5$ samples for the numeric
relations, consistency voting over $N{=}3$ samples at threshold $\theta{=}0.5$
for the null-heavy relations (Algorithm~\ref{alg:abstain}), and a single greedy
enumeration for \textsc{awardWonBy}. These self-consistency configurations
(temperature $\tau{=}0.7$) are evaluated on Qwen2.5-14B, where we can sample
freely. For Gemma-4-31B, API rate limits precluded multi-sampling at scale, so it
is decoded with a \emph{single} sample per query---yet it still outperforms the
14B (Section~\ref{sec:results}), indicating that model scale dominates
decode-time self-consistency at this budget. Prompts are 8-shot for every
relation except \textsc{awardWonBy} (2-shot), with exemplars from the training
split \citep{brown2020gpt3}.

\paragraph{Hardware and implementation.} Qwen2.5-14B runs on a single NVIDIA T4
(16\,GB) via HuggingFace Transformers in 4-bit (\texttt{bitsandbytes}
\texttt{nf4}, \texttt{float16} compute) \citep{dettmers2023qlora}; quantisation
only fits the hardware and does not change the counted parameter budget.
Gemma-4-31B is queried over its API from the same machine. Each model processes
the full 478-instance validation set.

\paragraph{Baselines.} We report two references on the same validation split: the
\emph{empty} baseline, which predicts $\emptyset$ for every instance
(Section~\ref{sec:task}), and the \emph{official} organiser baseline, a few-shot
prompting system whose per-relation scores we take from the task repository.

\paragraph{Reproducibility.} Generation writes a cache of every raw sample; all
post-processing---parsing, consistency voting, median aggregation, and the
threshold sweep---is a deterministic, GPU-free function of that cache. Every
ablation in Section~\ref{sec:analysis} therefore comes from a single generation
pass and reproduces without a GPU. We release all code and prompts.

\section{Results}
\label{sec:results}
Our primary system, \textbf{Gemma-4-31B}, scores \textbf{0.459}
relation-averaged macro-F\textsubscript{1} (\textbf{0.490} micro) on
validation---versus $0.175$/$0.203$ (empty), $0.273$/$0.284$ (official baseline),
and $0.416$/$0.447$ for the smaller Qwen2.5-14B. Figure~\ref{fig:bars} and
Table~\ref{tab:results} give the per-relation picture. Gemma-4-31B \emph{beats
the official baseline on all six relations} and the 14B on four of six; the
gains are largest on \textsc{companyTradesAtStockExchange} ($0.62\!\to\!0.72$)
and \textsc{hasCapacity} ($0.09\!\to\!0.17$, nearly doubled), while
\textsc{countryLandBordersCountry} reaches $0.953$. It ties the 14B on
\textsc{hasArea} and trails only on \textsc{personHasCityOfDeath}
($0.46$ vs.\ $0.48$)---the one relation where the single-sample 30B forgoes the
abstention voting the 14B uses. Strikingly, the 30B attains its lead
\emph{without} self-consistency or abstention, indicating that model scale
outweighs those decode-time mechanisms at this budget.

\begin{figure}[t]
\centering
\begin{tikzpicture}
\begin{axis}[ybar,width=\columnwidth,height=4.6cm,bar width=2.7pt,
 symbolic x coords={borders,company,death,area,capacity,award},
 xtick=data,ymin=0,ymax=1,ylabel={\small macro-F1},
 ylabel style={yshift=-2mm}, legend style={font=\tiny,at={(0.99,0.99)},anchor=north east,legend columns=2},
 enlarge x limits=0.12, x tick label style={font=\scriptsize,rotate=25,anchor=east}]
\addplot coordinates {(borders,0.25)(company,0.35)(death,0.45)(area,0)(capacity,0)(award,0)};
\addplot coordinates {(borders,0.679)(company,0.32)(death,0.16)(area,0.31)(capacity,0.10)(award,0.069)};
\addplot coordinates {(borders,0.90)(company,0.616)(death,0.48)(area,0.33)(capacity,0.09)(award,0.081)};
\addplot coordinates {(borders,0.953)(company,0.722)(death,0.46)(area,0.33)(capacity,0.17)(award,0.12)};
\legend{Empty,Baseline,14B,30B}
\end{axis}
\end{tikzpicture}
\caption{Per-relation validation macro-F\textsubscript{1}: predict-nothing
baseline, official baseline, Qwen2.5-14B, and Gemma-4-31B (30B). The 30B beats
the official baseline on all six relations.}
\label{fig:bars}
\end{figure}

\begin{table}[t]
\centering\footnotesize
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{lrrrr}
\toprule
Relation & Empty & Base & 14B & \textbf{30B} \\
\midrule
countryLandBorders  & 0.250 & 0.679 & 0.900 & \textbf{0.953} \\
companyTrades       & 0.350 & 0.320 & 0.616 & \textbf{0.722} \\
personCityOfDeath   & 0.450 & 0.160 & \textbf{0.480} & 0.460 \\
hasArea             & 0.000 & 0.310 & 0.330 & 0.330 \\
hasCapacity         & 0.000 & 0.100 & 0.090 & \textbf{0.170} \\
awardWonBy          & 0.000 & 0.069 & 0.081 & \textbf{0.120} \\
\midrule
Avg.\ of relations  & 0.175 & 0.273 & 0.416 & \textbf{0.459} \\
Micro (all inst.)   & 0.203 & 0.284 & 0.447 & \textbf{0.490} \\
\bottomrule
\end{tabular}
\caption{Validation macro-F\textsubscript{1} by model. ``Empty'' is the measured
predict-nothing baseline, ``Base'' the official baseline; \textbf{30B} is our
primary Gemma-4-31B system, 14B is Qwen2.5-14B. Best per row in bold.
Gemma-4-31B beats the official baseline on \emph{all six} relations.}
\label{tab:results}
\end{table}

\section{Analysis}
\label{sec:analysis}
All ablations are decoded from the same cached samples.

\paragraph{Abstention rescues weak models, refines strong ones.}
Table~\ref{tab:ablation} isolates abstention on the three null-heavy relations.
On a poorly-calibrated 0.5B model the effect is dramatic ($0.173\!\to\!0.372$): without voting it over-answers and falls \emph{below} the empty baseline ($0.350$). The 14B is already above empty without voting
($0.652$), so voting adds less ($0.652\!\to\!0.666$) but remains positive.
Abstention buys precision: on \textsc{personHasCityOfDeath} it moves
$P{=}0.77\!\to\!0.81\!\to\!0.89$ as $\theta$ rises, trading a little recall---it
suppresses wrong guesses, not correct answers.

\begin{table}[t]
\centering\small
\begin{tabular}{lrrr}
\toprule
 & Off & $\theta{=}0.5$ & $\theta{=}0.67$ \\
\midrule
0.5B (avg of 3 null-heavy) & 0.173 & 0.363 & \textbf{0.372} \\
14B (avg of 3 null-heavy)  & 0.652 & 0.665 & \textbf{0.666} \\
\bottomrule
\end{tabular}
\caption{Self-consistency abstention, relation-averaged over the three
null-heavy relations only. ``Off'' decodes a single sample.}
\label{tab:ablation}
\end{table}

\paragraph{Threshold tracks the empty rate; numeric voting is neutral.}
If abstention is what drives the string relations, the natural question is how its threshold should be set.
Figure~\ref{fig:ablate} (left) sweeps $\theta$: the most null-heavy relation,
\textsc{personHasCityOfDeath} (45\% empty), peaks at a \emph{lower} $\theta$,
while the others tolerate aggressive voting---arguing for per-relation
thresholds. Figure~\ref{fig:ablate} (right) varies the number of numeric
samples: median aggregation is non-monotonic on \textsc{hasArea} and actually
slightly \emph{hurts} \textsc{hasCapacity} (single sample $0.10$ vs.\ median-5
$0.09$). Decode variance is not the bottleneck---the gap is knowledge-bound; the
model's capacity guesses are the right order of magnitude but outside the 5\%
band (e.g.\ Spartan Stadium: predicted $37{,}000$, gold $75{,}005$).

\begin{figure*}[t]
\centering
\begin{tikzpicture}
\begin{axis}[name=ax1,width=7.6cm,height=4.6cm,xlabel={$\theta$ (voting threshold)},
 ylabel={macro-F1},
 ymin=0.4,ymax=0.95,xtick={0,0.34,0.5,0.67,1},tick label style={font=\small},
 title style={font=\small},title={string relations},
 legend style={font=\scriptsize,at={(0.5,-0.26)},anchor=north,legend columns=3}]
\addplot coordinates {(0,0.902)(0.34,0.900)(0.5,0.900)(0.67,0.907)(1,0.907)};
\addplot coordinates {(0,0.603)(0.34,0.616)(0.5,0.616)(0.67,0.621)(1,0.621)};
\addplot coordinates {(0,0.450)(0.34,0.480)(0.5,0.480)(0.67,0.470)(1,0.470)};
\legend{borders,company,death}
\end{axis}
\begin{axis}[at={(9cm,0)},width=7.6cm,height=4.6cm,xlabel={\#samples $n$ (median)},
 ylabel={macro-F1},ymin=0,ymax=0.4,xtick={1,2,3,4,5},tick label style={font=\small},
 title style={font=\small},title={numeric relations},
 legend style={font=\scriptsize,at={(0.5,-0.26)},anchor=north,legend columns=2}]
\addplot coordinates {(1,0.30)(2,0.28)(3,0.31)(4,0.30)(5,0.33)};
\addplot coordinates {(1,0.10)(2,0.04)(3,0.08)(4,0.06)(5,0.09)};
\legend{hasArea,hasCapacity}
\end{axis}
\end{tikzpicture}
\caption{Left: per-relation F\textsubscript{1} vs.\ voting threshold $\theta$
(14B); the most null-heavy relation (\textsc{personHasCityOfDeath}, 45\% empty) peaks at a lower
$\theta$ than the others. Right: numeric F\textsubscript{1} vs.\ number of median
samples $n$; aggregation is roughly neutral, indicating a knowledge-bound gap.}
\label{fig:ablate}
\end{figure*}

\paragraph{\textsc{awardWonBy} is recall-capped.} The one relation we never beat the baseline on comfortably exposes a different limit. Gold sets average ${\sim}148$
recipients (max 641) but the 14B enumerates only ${\sim}11$ names---its outputs
are \emph{not} truncated (it closes the JSON early)---capping recall near
$11/148\approx0.07$. Listed names are often plausible-but-wrong ($P{=}0.36$).
Parametric recall and willingness to enumerate bound this relation, where a larger in-budget model or multi-step decomposition would help most.

\section{Conclusion}
Every component of our system follows from a single metric analysis: abstention is valuable,
numeric recall is scarce, and scope grounding curbs over- and under-prediction.
With Gemma-4-31B it reaches $0.459$ relation-averaged macro-F\textsubscript{1}
($0.490$ micro), beating the official baseline on all six relations (0.416 with a
local 4-bit Qwen2.5-14B), and is fully reproducible from a cached generation
pass. That the 30B wins with single-sample decoding, while abstention and
self-consistency chiefly help the smaller models, suggests that model scale and
decode-time calibration are complementary levers whose balance shifts with model
strength.

\section*{Limitations}
The voting threshold $\theta$ is swept on the same validation split it is
evaluated on, so per-relation optima are optimistic and not held-out; we report
no test numbers yet. Several margins are within plausible decoding noise:
\textsc{awardWonBy} has only 10 subjects, and we did not measure
run-to-run variance of the stochastic decoding ($N{=}3$, $n{=}5$), so
sub-$0.03$ deltas (e.g.\ the \textsc{personHasCityOfDeath} $+0.03$) are
indicative only. Being closed-book, the system inherits the base model's
coverage and recency limits and cannot recover facts it never stored, which caps
\textsc{awardWonBy} recall and rare-venue numeric accuracy. Because API rate
limits precluded multi-sample decoding for Gemma-4-31B, its scores are
single-sample; pairing the 30B with the self-consistency and abstention voting
that help the smaller models is left to future work and may lift the null-heavy
and numeric relations further. Finally, the
abstention advantage is a property of this task's macro metric and the
empty-as-valid-answer setting, and may not transfer to single-answer or
micro-only settings.

\section*{Ethics and Generative-AI Declaration}
Closed-book KB construction surfaces facts from model parameters that may be
stale, biased, or hallucinated; predictions about people (e.g.\ place of death)
can be wrong and should not be treated as authoritative without verification. The
abstention mechanism partly mitigates this by emitting an empty answer rather
than a confident fabrication when the model is uncertain. The only generative-AI
component is the open-weight LM used for the predictions themselves, as the task
requires; no proprietary assistant was used to produce results.

\bibliography{refs}

\end{document}
